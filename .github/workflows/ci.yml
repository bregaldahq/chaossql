name: ChaosSQL CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  test:
    name: Verification & Tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: 'stable'
        cache: true

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Validate Harness Artifacts
      run: make check-harness

    - name: Verify English Purity Gate
      run: node tools/test_english_purity.js

    - name: Run Linter & Vet
      run: make lint

    - name: Run Unit & Concurrency Tests (-race)
      run: make test

    - name: Build Binary (Zero CGO)
      run: make build

    - name: Run Interactive Demo Battery
      run: make demo
