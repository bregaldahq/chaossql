apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaossql-server.fullname" . }}
  labels:
    {{- include "chaossql-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chaossql-server.selectorLabels" . | nindent 4 }}
